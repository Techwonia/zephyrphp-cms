{% extends "@cms/layouts/cms.twig" %}

{% block title %}Edit Email Template{% endblock %}

{% block content %}
<div class="page-header" style="display: flex; justify-content: space-between; align-items: center; margin-bottom: var(--sp-6);">
    <div>
        <h1 style="margin: 0;">Edit: {{ template.getName() }}</h1>
        <p style="color: var(--text-secondary); margin: var(--sp-1) 0 0;">Slug: <code style="font-size: var(--text-xs); background: var(--bg-subdued); padding: 2px 6px; border-radius: var(--radius-sm);">{{ template.getSlug() }}</code></p>
    </div>
    <div style="display: flex; gap: var(--sp-2);">
        <a href="{{ admin_url('email-templates/' ~ template.getId() ~ '/preview') }}" target="_blank" class="btn btn-outline btn-sm">Preview</a>
        <button onclick="testSend({{ template.getId() }})" class="btn btn-outline btn-sm">Test Send</button>
        <a href="{{ admin_url('email-templates') }}" class="btn btn-outline btn-sm">Back</a>
    </div>
</div>

<form method="POST" action="{{ admin_url('email-templates/' ~ template.getId()) }}" class="card">
    <input type="hidden" name="_token" value="{{ csrf_token() }}">

    <div style="padding: var(--sp-5);">
        <div class="form-group" style="margin-bottom: var(--sp-4);">
            <label class="form-label">Name</label>
            <input type="text" name="name" class="form-control" value="{{ template.getName() }}" required>
        </div>

        <div class="form-group" style="margin-bottom: var(--sp-4);">
            <label class="form-label">Subject Line</label>
            <input type="text" name="subject" class="form-control" value="{{ template.getSubject() }}" required>
            <small style="color: var(--text-secondary);">Use Twig variables: {{ '{{ app_name }}' }}, {{ '{{ entry_title }}' }}, {{ '{{ collection_name }}' }}, {{ '{{ user_name }}' }}</small>
        </div>

        <div class="form-group" style="margin-bottom: var(--sp-4);">
            <label class="form-label">Body (Twig HTML)</label>
            <textarea name="body_twig" class="form-control" rows="15" style="font-family: var(--font-mono); font-size: var(--text-sm);" required>{{ template.getBodyTwig() }}</textarea>
            <small style="color: var(--text-secondary);">
                Available variables: {{ '{{ app_name }}' }}, {{ '{{ entry_title }}' }}, {{ '{{ collection_name }}' }}, {{ '{{ entry_url }}' }}, {{ '{{ admin_url }}' }}, {{ '{{ user_name }}' }}, {{ '{{ user_email }}' }}, {{ '{{ fields }}' }}
            </small>
        </div>

        <div class="form-group" style="margin-bottom: var(--sp-4);">
            <label style="display: flex; align-items: center; gap: var(--sp-2); cursor: pointer;">
                <input type="checkbox" name="is_active" value="1" {% if template.isActive() %}checked{% endif %}>
                <span>Active</span>
            </label>
        </div>
    </div>

    <div style="padding: var(--sp-4) var(--sp-5); border-top: 1px solid var(--border); display: flex; gap: var(--sp-2);">
        <button type="submit" class="btn btn-primary">Save Template</button>
        <a href="{{ admin_url('email-templates') }}" class="btn btn-outline">Cancel</a>
    </div>
</form>

<script nonce="{{ csp_nonce() }}">
function testSend(id) {
    if (!confirm('Send a test email to your account email address?')) return;
    fetch('/admin/email-templates/' + id + '/test', {
        method: 'POST',
        headers: {
            'X-Requested-With': 'XMLHttpRequest',
            'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').content
        }
    })
    .then(r => r.json())
    .then(data => {
        alert(data.message || data.error || 'Done.');
    })
    .catch(() => alert('Request failed.'));
}
</script>
{% endblock %}
