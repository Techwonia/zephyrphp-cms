{% extends "@cms/layouts/cms.twig" %}

{% block title %}Site Analytics{% endblock %}

{% block content %}
<div class="breadcrumb">
    <a href="{{ admin_url() }}">Dashboard</a> / Analytics
</div>

<div class="page-header" style="display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: var(--space-300);">
    <h1>Site Analytics</h1>
    <div style="display: flex; align-items: center; gap: var(--space-200);">
        {% if liveVisitors is defined %}
        <div style="display: flex; align-items: center; gap: var(--space-200); padding: var(--space-200) var(--space-400); background: var(--color-success-bg); border-radius: var(--radius-full); font-size: var(--font-sm);">
            <span style="width: 8px; height: 8px; background: var(--color-success); border-radius: 50%; display: inline-block; animation: livePulse 2s infinite;"></span>
            <strong style="color: var(--color-success);">{{ liveVisitors }}</strong>
            <span style="color: var(--text-secondary);">live now</span>
        </div>
        {% endif %}
        <div class="analytics-range-picker" style="display: flex; border: 1px solid var(--border); border-radius: var(--radius-200); overflow: hidden;">
            <a href="?range=today" class="range-btn{% if range == 'today' %} active{% endif %}" style="padding: var(--space-200) var(--space-400); font-size: var(--font-sm); text-decoration: none; color: var(--text); border-right: 1px solid var(--border); {% if range == 'today' %}background: var(--color-primary); color: var(--color-primary-text);{% else %}background: var(--bg-surface);{% endif %}">Today</a>
            <a href="?range=7d" class="range-btn{% if range == '7d' %} active{% endif %}" style="padding: var(--space-200) var(--space-400); font-size: var(--font-sm); text-decoration: none; color: var(--text); border-right: 1px solid var(--border); {% if range == '7d' %}background: var(--color-primary); color: var(--color-primary-text);{% else %}background: var(--bg-surface);{% endif %}">7 Days</a>
            <a href="?range=30d" class="range-btn{% if range == '30d' %} active{% endif %}" style="padding: var(--space-200) var(--space-400); font-size: var(--font-sm); text-decoration: none; color: var(--text); border-right: 1px solid var(--border); {% if range == '30d' %}background: var(--color-primary); color: var(--color-primary-text);{% else %}background: var(--bg-surface);{% endif %}">30 Days</a>
            <a href="?range=90d" class="range-btn{% if range == '90d' %} active{% endif %}" style="padding: var(--space-200) var(--space-400); font-size: var(--font-sm); text-decoration: none; color: var(--text); {% if range == '90d' %}background: var(--color-primary); color: var(--color-primary-text);{% else %}background: var(--bg-surface);{% endif %}">90 Days</a>
        </div>
    </div>
</div>

{% if error is defined and error %}
<div class="card" style="margin-bottom: var(--space-500);">
    <div class="card-body" style="color: var(--color-critical);">
        <strong>Error:</strong> {{ error }}
    </div>
</div>
{% else %}

{# ── Stats Cards ── #}
<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: var(--space-400); margin-bottom: var(--space-500);">
    <div class="card">
        <div class="card-body" style="text-align: center; padding: var(--space-400);">
            <div style="font-size: 2rem; font-weight: 700; color: var(--color-primary);">{{ totalRange|number_format }}</div>
            <div style="color: var(--text-secondary); font-size: var(--font-sm);">Page Views</div>
            <div style="color: var(--text-secondary); font-size: var(--font-xs); margin-top: var(--space-100);">
                Today: {{ totalToday|number_format }} &middot; 7d: {{ total7d|number_format }} &middot; 30d: {{ total30d|number_format }}
            </div>
        </div>
    </div>
    <div class="card">
        <div class="card-body" style="text-align: center; padding: var(--space-400);">
            <div style="font-size: 2rem; font-weight: 700; color: var(--color-info);">{{ uniqueRange|number_format }}</div>
            <div style="color: var(--text-secondary); font-size: var(--font-sm);">Unique Visitors</div>
            <div style="color: var(--text-secondary); font-size: var(--font-xs); margin-top: var(--space-100);">
                Today: {{ uniqueToday|number_format }} &middot; 7d: {{ unique7d|number_format }} &middot; 30d: {{ unique30d|number_format }}
            </div>
        </div>
    </div>
    <div class="card">
        <div class="card-body" style="text-align: center; padding: var(--space-400);">
            <div style="font-size: 2rem; font-weight: 700; color: {% if bounceRate > 70 %}var(--color-warning){% elseif bounceRate > 50 %}var(--text){% else %}var(--color-success){% endif %};">{{ bounceRate }}%</div>
            <div style="color: var(--text-secondary); font-size: var(--font-sm);">Bounce Rate</div>
            <div style="color: var(--text-secondary); font-size: var(--font-xs); margin-top: var(--space-100);">
                Single-page visits
            </div>
        </div>
    </div>
    <div class="card">
        <div class="card-body" style="text-align: center; padding: var(--space-400);">
            <div style="font-size: 2rem; font-weight: 700; color: var(--color-primary);">{{ avgPagesPerSession }}</div>
            <div style="color: var(--text-secondary); font-size: var(--font-sm);">Pages / Session</div>
            <div style="color: var(--text-secondary); font-size: var(--font-xs); margin-top: var(--space-100);">
                Average depth
            </div>
        </div>
    </div>
</div>

{# ── Traffic Chart (SVG Polyline) ── #}
<div class="card" style="margin-bottom: var(--space-500);">
    <div class="card-header" style="display: flex; align-items: center; justify-content: space-between;">
        <h3 style="margin:0; font-size:1rem;">Traffic Overview</h3>
        <div style="display: flex; align-items: center; gap: var(--space-400); font-size: var(--font-xs); color: var(--text-secondary);">
            <span style="display: flex; align-items: center; gap: var(--space-100);"><span style="width: 12px; height: 3px; background: var(--color-primary); border-radius: 2px; display: inline-block;"></span> Page Views</span>
            <span style="display: flex; align-items: center; gap: var(--space-100);"><span style="width: 12px; height: 3px; background: var(--color-success); border-radius: 2px; display: inline-block;"></span> Visitors</span>
        </div>
    </div>
    <div class="card-body" style="padding: var(--space-400);">
        {% if trafficChart|length > 0 %}
            {% set max_views = 1 %}
            {% for day in trafficChart %}
                {% if day.views > max_views %}{% set max_views = day.views %}{% endif %}
            {% endfor %}

            {% set chart_w = 900 %}
            {% set chart_h = 220 %}
            {% set pad_x = 50 %}
            {% set pad_y = 20 %}
            {% set inner_w = chart_w - pad_x - 10 %}
            {% set inner_h = chart_h - pad_y - 30 %}
            {% set step_x = trafficChart|length > 1 ? inner_w / (trafficChart|length - 1) : inner_w %}

            <div style="overflow-x: auto;">
            <svg viewBox="0 0 {{ chart_w }} {{ chart_h }}" style="width: 100%; height: auto; min-height: 200px; max-height: 300px;" xmlns="http://www.w3.org/2000/svg">
                {# Grid lines #}
                {% for i in 0..4 %}
                    {% set y_pos = pad_y + (inner_h * i / 4) %}
                    {% set label_val = ((max_views * (4 - i) / 4))|round %}
                    <line x1="{{ pad_x }}" y1="{{ y_pos }}" x2="{{ chart_w - 10 }}" y2="{{ y_pos }}" stroke="#E3E3E3" stroke-width="1" stroke-dasharray="4,4"/>
                    <text x="{{ pad_x - 8 }}" y="{{ y_pos + 4 }}" fill="#616161" font-size="10" text-anchor="end" font-family="Inter, sans-serif">{{ label_val }}</text>
                {% endfor %}

                {# Views area fill #}
                {% set views_points = '' %}
                {% set area_points = '' %}
                {% for day in trafficChart %}
                    {% set x = pad_x + (loop.index0 * step_x) %}
                    {% set y = pad_y + inner_h - (day.views / max_views * inner_h) %}
                    {% set views_points = views_points ~ x ~ ',' ~ y ~ ' ' %}
                    {% if loop.first %}
                        {% set area_points = x ~ ',' ~ (pad_y + inner_h) ~ ' ' %}
                    {% endif %}
                    {% set area_points = area_points ~ x ~ ',' ~ y ~ ' ' %}
                    {% if loop.last %}
                        {% set area_points = area_points ~ x ~ ',' ~ (pad_y + inner_h) %}
                    {% endif %}
                {% endfor %}
                <polygon points="{{ area_points }}" fill="var(--color-primary)" opacity="0.08"/>
                <polyline points="{{ views_points }}" fill="none" stroke="var(--color-primary)" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>

                {# Visitors line #}
                {% set visitor_points = '' %}
                {% for day in trafficChart %}
                    {% set x = pad_x + (loop.index0 * step_x) %}
                    {% set y = pad_y + inner_h - (day.visitors / max_views * inner_h) %}
                    {% set visitor_points = visitor_points ~ x ~ ',' ~ y ~ ' ' %}
                {% endfor %}
                <polyline points="{{ visitor_points }}" fill="none" stroke="var(--color-success)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" stroke-dasharray="6,3"/>

                {# Data point dots for views #}
                {% for day in trafficChart %}
                    {% set x = pad_x + (loop.index0 * step_x) %}
                    {% set y = pad_y + inner_h - (day.views / max_views * inner_h) %}
                    <circle cx="{{ x }}" cy="{{ y }}" r="3" fill="var(--color-primary)" stroke="white" stroke-width="1.5">
                        <title>{{ day.date }}: {{ day.views }} views, {{ day.visitors }} visitors</title>
                    </circle>
                {% endfor %}

                {# X-axis labels #}
                {% for day in trafficChart %}
                    {% if trafficChart|length <= 14 or loop.index0 % (trafficChart|length / 7)|round(0, 'ceil') == 0 or loop.last %}
                        {% set x = pad_x + (loop.index0 * step_x) %}
                        <text x="{{ x }}" y="{{ chart_h - 5 }}" fill="#616161" font-size="10" text-anchor="middle" font-family="Inter, sans-serif">{{ day.date|slice(5) }}</text>
                    {% endif %}
                {% endfor %}
            </svg>
            </div>
        {% else %}
            <div style="text-align: center; padding: var(--space-800); color: var(--text-secondary);">
                No traffic data available for this period. Analytics data is collected by the tracking middleware.
            </div>
        {% endif %}
    </div>
</div>

{# ── Top Pages & Referrer Sources ── #}
<div style="display: grid; grid-template-columns: 3fr 2fr; gap: var(--space-400); margin-bottom: var(--space-500);">
    <div class="card">
        <div class="card-header"><h3 style="margin:0; font-size:1rem;">Top Pages</h3></div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>Page</th>
                            <th style="text-align: right;">Views</th>
                            <th style="width: 120px;"></th>
                        </tr>
                    </thead>
                    <tbody>
                        {% set max_page_views = topPages[0].views|default(1) %}
                        {% for page in topPages %}
                        <tr>
                            <td><code style="font-size: var(--font-sm);">{{ page.path|length > 60 ? page.path[:60] ~ '...' : page.path }}</code></td>
                            <td style="text-align: right; font-weight: 600; white-space: nowrap;">{{ page.views|number_format }}</td>
                            <td>
                                <div style="background: var(--bg-subdued); border-radius: var(--radius-100); height: 6px; overflow: hidden;">
                                    <div style="background: var(--color-primary); height: 100%; width: {{ (page.views / max_page_views * 100)|round }}%; border-radius: var(--radius-100);"></div>
                                </div>
                            </td>
                        </tr>
                        {% else %}
                        <tr><td colspan="3" style="text-align: center; color: var(--text-secondary);">No page data</td></tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-header"><h3 style="margin:0; font-size:1rem;">Referrer Sources</h3></div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>Source</th>
                            <th style="text-align: right;">Visits</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% set max_ref = topReferrers[0].count|default(1) %}
                        {% for ref in topReferrers %}
                        <tr>
                            <td>
                                {% if ref.source == 'Direct / None' %}
                                    <span style="color: var(--text-secondary);">{{ ref.source }}</span>
                                {% else %}
                                    {{ ref.source }}
                                {% endif %}
                            </td>
                            <td style="text-align: right;">
                                <div style="display: flex; align-items: center; justify-content: flex-end; gap: var(--space-200);">
                                    <div style="width: 60px; background: var(--bg-subdued); border-radius: var(--radius-100); height: 6px; overflow: hidden;">
                                        <div style="background: var(--color-info); height: 100%; width: {{ (ref.count / max_ref * 100)|round }}%; border-radius: var(--radius-100);"></div>
                                    </div>
                                    <span style="font-weight: 600; min-width: 30px;">{{ ref.count|number_format }}</span>
                                </div>
                            </td>
                        </tr>
                        {% else %}
                        <tr><td colspan="2" style="text-align: center; color: var(--text-secondary);">No referrer data</td></tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

{# ── Browser & Device Breakdown + Content Sections ── #}
<div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: var(--space-400); margin-bottom: var(--space-500);">
    {# Browser Breakdown #}
    <div class="card">
        <div class="card-header"><h3 style="margin:0; font-size:1rem;">Browsers</h3></div>
        <div class="card-body" style="padding: var(--space-400);">
            {% set max_browser = browserBreakdown[0].count|default(1) %}
            {% set browser_colors = ['#1A7BA8', '#047B5D', '#B98900', '#D72C0D', '#2C6ECB', '#6D28D9', '#DB2777', '#EA580C'] %}
            {% for browser in browserBreakdown %}
            <div style="margin-bottom: var(--space-300);">
                <div style="display: flex; justify-content: space-between; font-size: var(--font-sm); margin-bottom: var(--space-100);">
                    <span>{{ browser.name }}</span>
                    <span style="font-weight: 600;">{{ browser.count|number_format }}{% if totalRange > 0 %} <span style="color: var(--text-secondary); font-weight: 400;">({{ ((browser.count / totalRange) * 100)|round(1) }}%)</span>{% endif %}</span>
                </div>
                <div style="background: var(--bg-subdued); border-radius: var(--radius-100); height: 8px; overflow: hidden;">
                    <div style="background: {{ browser_colors[loop.index0] ?? '#1A7BA8' }}; height: 100%; width: {{ (browser.count / max_browser * 100)|round }}%; border-radius: var(--radius-100); transition: width 0.3s ease;"></div>
                </div>
            </div>
            {% else %}
            <div style="text-align: center; color: var(--text-secondary); padding: var(--space-400);">No browser data</div>
            {% endfor %}
        </div>
    </div>

    {# Device Breakdown #}
    <div class="card">
        <div class="card-header"><h3 style="margin:0; font-size:1rem;">Devices</h3></div>
        <div class="card-body" style="padding: var(--space-400);">
            {% set device_icons = {'Desktop': 'M4 6h16v10H4zm2 12h12', 'Mobile': 'M7 2h10v20H7z', 'Tablet': 'M6 2h12v20H6z'} %}
            {% set device_colors = {'Desktop': '#1A7BA8', 'Mobile': '#047B5D', 'Tablet': '#B98900'} %}
            {% set max_device = deviceBreakdown[0].count|default(1) %}

            {# SVG Donut Chart #}
            {% if deviceBreakdown|length > 0 %}
            <div style="display: flex; justify-content: center; margin-bottom: var(--space-400);">
                <svg viewBox="0 0 120 120" style="width: 120px; height: 120px;">
                    {% set offset = 0 %}
                    {% set total = 0 %}
                    {% for device in deviceBreakdown %}
                        {% set total = total + device.count %}
                    {% endfor %}
                    {% for device in deviceBreakdown %}
                        {% set pct = total > 0 ? (device.count / total * 100) : 0 %}
                        {% set dashLen = pct * 2.827 %}
                        {% set dashGap = 282.7 - dashLen %}
                        <circle cx="60" cy="60" r="45" fill="none" stroke="{{ device_colors[device.name] ?? '#999' }}" stroke-width="16" stroke-dasharray="{{ dashLen }} {{ dashGap }}" stroke-dashoffset="{{ -offset * 2.827 }}" transform="rotate(-90 60 60)" opacity="0.85"/>
                        {% set offset = offset + pct %}
                    {% endfor %}
                    <text x="60" y="56" text-anchor="middle" font-size="14" font-weight="700" fill="var(--text)" font-family="Inter, sans-serif">{{ totalRange|number_format }}</text>
                    <text x="60" y="70" text-anchor="middle" font-size="9" fill="var(--text-secondary)" font-family="Inter, sans-serif">total</text>
                </svg>
            </div>
            {% endif %}

            {% for device in deviceBreakdown %}
            <div style="display: flex; align-items: center; justify-content: space-between; padding: var(--space-200) 0; {% if not loop.last %}border-bottom: 1px solid var(--border);{% endif %}">
                <div style="display: flex; align-items: center; gap: var(--space-200);">
                    <span style="width: 10px; height: 10px; background: {{ device_colors[device.name] ?? '#999' }}; border-radius: 50%; display: inline-block;"></span>
                    <span style="font-size: var(--font-sm);">{{ device.name }}</span>
                </div>
                <div style="font-size: var(--font-sm);">
                    <strong>{{ device.count|number_format }}</strong>
                    {% if totalRange > 0 %}
                    <span style="color: var(--text-secondary);">({{ ((device.count / totalRange) * 100)|round(1) }}%)</span>
                    {% endif %}
                </div>
            </div>
            {% else %}
            <div style="text-align: center; color: var(--text-secondary); padding: var(--space-400);">No device data</div>
            {% endfor %}
        </div>
    </div>

    {# Content Sections #}
    <div class="card">
        <div class="card-header"><h3 style="margin:0; font-size:1rem;">Content Sections</h3></div>
        <div class="card-body" style="padding: var(--space-400);">
            {% set max_section = topSections[0].views|default(1) %}
            {% set section_colors = ['#1A7BA8', '#2C6ECB', '#047B5D', '#6D28D9', '#B98900', '#DB2777', '#EA580C', '#0D9488', '#7C3AED', '#DC2626'] %}
            {% for section in topSections %}
            <div style="margin-bottom: var(--space-300);">
                <div style="display: flex; justify-content: space-between; font-size: var(--font-sm); margin-bottom: var(--space-100);">
                    <span style="text-transform: capitalize;">/{{ section.section }}</span>
                    <span style="font-weight: 600;">{{ section.views|number_format }}</span>
                </div>
                <div style="background: var(--bg-subdued); border-radius: var(--radius-100); height: 8px; overflow: hidden;">
                    <div style="background: {{ section_colors[loop.index0] ?? '#1A7BA8' }}; height: 100%; width: {{ (section.views / max_section * 100)|round }}%; border-radius: var(--radius-100);"></div>
                </div>
            </div>
            {% else %}
            <div style="text-align: center; color: var(--text-secondary); padding: var(--space-400);">No section data</div>
            {% endfor %}
        </div>
    </div>
</div>

{% endif %}

<style nonce="{{ csp_nonce() }}">
@keyframes livePulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.4; }
}
</style>
{% endblock %}
